<!DOCTYPE html>
<html>

<head>
    <title>編輯會員資料</title>
    <style>
        input {margin:0.5rem 0;}
        label>input {color:rgb(77, 77, 77);font-weight:500;}
    </style>
</head>
<% include header %>
<body>
<form id="mainForm" action="/member/edit1" method="POST">
    <div class="container">
        <ol class="breadcrumb">
            <li><a href="/">首頁</a></li>
            <li><a href="/member">會員中心</a></li>
            <li class="active">編輯會員資料</li>
        </ol>
    </div>
    <div class="col-lg-12">
        <h2 class="top">編輯會員資料</h2>
    </div>
    <% if (loginStatus == false) { %>
        <% include loginlayout %>
    <% } else if(loginStatus == true) { %>
    <section class="col-lg-4 col-lg-offset-3">
        <label>中文全名：
            <input type="text" value="<%= data[0].username %>" name="username" class="form-control">
        </label><br>
        <label>E-mail：
            <input type="text" value="<%= data[0].email %>" class="form-control" disabled>
        </label><br>
        <label>室內電話：
            <input type="text" value="<%= data[0].tel %>" name="tel" class="form-control">
        </label><br>
        <label>生日：
            <input type="date" value="<%= moment(data[0].birth).format('YYYY-MM-DD') %>" name="birth" class="form-control">
        </label><br>
        <label>地址：
            <input type="text" value="<%= data[0].address %>" name="address" class="form-control">
        </label><br>
        <input type="submit" value="送出" class="btn btn-primary">
    </section>
    <% } %>
</form>
<script>
    $(function () {
        $('.memenu li').eq(5).addClass('active');
    })
</script>
</body>
</html>