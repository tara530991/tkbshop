<!DOCTYPE html>
<html>
<head>
    <title>結帳</title>
    <style>
        #total,#total+span {font-size: 2rem;}
        #checkform {margin:0 250px;}
        #checkform span {display:inline-block;margin:0.5rem;} 
    </style>
</head>
<% include header %>
<body>
    <form action="/product/check1" method="POST">
    <div id="wrapper">
        <div id="page">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">結帳</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
        </div>
    </div>
    <!-- /.row -->
    <div class="row wrap1280">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    DataTables Advanced Tables
                </div>
                <!-- /.panel-heading -->
                    <div id="panBody" class="panel-body">
                        <div id="cart_list">
                            <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                                <thead>
                                    <tr>
                                        <th>品名</th>
                                        <th>售價</th>
                                        <th>購買數量</th>
                                        <th>合計金額</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% for(var i=0 ; i < data.length;i++) { %>
                                        <tr class="odd gradeX">
                                            <td>
                                                <%=data[i].product_name %>
                                            </td>
                                            <td>
                                                <%=data[i].price %>
                                            </td>
                                            <td class="amount">
                                                <%=data[i].PRODUCT_AMOUNT %>
                                            </td>
                                            <td class="subtotal">
                                                <%=data[i].subtotal %>
                                            </td>
                                        </tr>
                                        <% } %>
                                </tbody>
                            </table>
                            <div>
                                <span id="total">總價：<input type="hidden" name="total"></span>
                                <span>元</span>
                                
                            </div>
                        </div>
                <!-- /.table-responsive -->
                </div>
                <!-- /.panel-body -->
            </div>
            <!-- /.panel -->
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <div id="checkform">
        <span>收件人名稱：</span><input type="text" name="buyer" required><br>
        <span>收件地址：</span><input type="text" name="address" required><br>
        <span>發票類型：</span>
        <input type="radio" name="receipt" value="2"><span>二聯式</span>
        <input type="radio" name="receipt" value="3"><span id="tip">三聯式</span><br>
        <span>發票抬頭：</span><input type="text" name="invoiceTitle"><br>
        <span>統一編號：</span><input type="text" name="invoiceNumber"><br>
        <span>付款方式：</span><input type="radio" name="payMethod" value="1" checked><span>ATM轉帳</span><br>
        <h4>敬請確認無誤後再將資料送出</h4>
        <input type="button" value="確認結帳" onclick="formCheck(this)">
        
    </div>
    </form>
    <script>
        $(function () {
            var total = 0;
            $(".subtotal").each(function (i) {
                total += parseInt($(this).text());
                // var total = [];
                return total;
            });
            $("#total").append("<span id='totalVal'>" + total + "</span>");
            $("#total>input").val(total);
            console.log($("#total>input").val(total));
        });
        function formCheck(k){
            var receipt = $('[name="receipt"]:checked').val();
            if(receipt == undefined){
                $('<span style="color:red;">必填</span>').appendTo('#tip');
            }else if(receipt == 2 || receipt == 3){
                $(k).attr('type','submit');
                $(k).submit();
                // $.ajax({
                //     url:'/product/check1',
                //     method: 'POST',
                //     datatype: 'text',
                //     data:{
                //         total: $('#totalVal').text(),
                //     }
                // })
            }
        }

    </script>
</body>
</html>