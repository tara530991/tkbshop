<!DOCTYPE html>
<html>
<head>
    <title>消息內容修改-後台管理</title>
    <style>
        img {max-height:80px;max-width:130px;margin:0.5em 0;}
        input[type=submit]{margin-bottom:5em;}
    </style>
</head>
<% include backheader %>
<body>
<form action="/admin/newsModify1" method="POST" enctype="multipart/form-data">
<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">消息內容修改</h1>
        </div>
    </div>
    <section>
        <input type="hidden" name="newsId" value="<%=data[0].id %>">
        <label>標題：
            <input type="text" name="title" value="<%=data[0].title %>" class="form-control"/>
        </label><br>
        <label>日期：<input type="date" name="date" value="<%=moment(data[0].date).format('YYYY-MM-DD') %>" class="form-control">
        </label><br>
        <label>圖片：<br>
            <span>原始圖片：</span>
            <% if (data[0].pic !== "") { %>
                <img id="showpic" src="/upload/<%=data[0].pic %>" alt=""><br>
            <% } else { %>
                <span>尚未上傳圖片</span><br>
            <% } %>
            <span>更換圖片：</span>
            <input type="file" name="pic">
        </label><br>
        <label>內容：</label><br>
            <textarea name="content" rows="50" class="form-control">
                <%-data[0].content %>
            </textarea><br>
            <script>
                CKEDITOR.replace('content', {
                    height:400,
                    width:800,
                });
                    //()內的第一個參數會去對照textarea中的name
                    //{}則是客製化設定
            </script>
        <label>狀態設定：</label>
        <select name="status" class="form-control">
            <option value="1">上架中</option>
            <option value="2">下架中(前台無法顯示、搜尋)</option>
        </select><br>
        <input type="submit" value="送出" class="btn btn-default">            
    </section>
</div>
</form>

<script>
    $(function(){

    })
</script>
</body>
</html>