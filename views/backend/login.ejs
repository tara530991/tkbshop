<!DOCTYPE html>
<html>
<head>
    <title>登錄後台管理</title>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Bootstrap Core CSS -->
    <link href="/backvendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <!-- MetisMenu CSS -->
    <link href="/backvendor/metisMenu/metisMenu.min.css" rel="stylesheet">
    <!-- Social Buttons CSS -->
    <link href="/backvendor/bootstrap-social/bootstrap-social.css" rel="stylesheet">
    <!-- Custom CSS -->
    <link href="/backvendor/dist/css/sb-admin-2.css" rel="stylesheet">
    <!-- Custom Fonts -->
    <link href="/backvendor/font-awesome/css/font-awesome.min.css" rel="stylesheet" type="text/css">
    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
        <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body>
    <div class="container">
            <div class="row">
                <div class="col-md-4 col-md-offset-4">
                    <div class="login-panel panel panel-default">
                        <div class="panel-heading">
                            <h3 class="panel-title">管理員登入</h3>
                        </div>
                        <div class="panel-body">
                            <fieldset>
                                    <% if (loginStatus == false) { %>
                                    <form role="form" action="/admin/login" method="POST">
                                        <div class="form-group">
                                        <input class="form-control" placeholder="帳號" name="account" type="text" autofocus>
                                        </div>
                                        <div class="form-group">
                                            <input class="form-control" placeholder="密碼" name="password" type="password">
                                        </div>
                                        <!-- <div class="checkbox">
                                            <label>
                                                <input name="remember" type="checkbox" value="Remember Me">Remember Me
                                            </label>
                                        </div> -->
                                        <input type="submit" value="登入" class="btn btn-lg btn-success btn-block">
                                        <%- message %>
                                    </form>
                                    <% } else if(loginStatus == true) { %>
                                        <form action="/admin/logout" method="get">
                                            <input type="submit" value="登出" class="btn btn-lg btn-warning btn-block">
                                        </form>
                                    <!-- Change this to a button or input when using this as a form -->
                                    <% } %>
                                </fieldset>
                        </div>
                    </div>
                </div>
            </div>
        </div>
</body>
</html>