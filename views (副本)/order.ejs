<!DOCTYPE html>
<html lang="en">
<head>
    <title>訂單查詢結果</title>
</head>
<% include header %>
<body>
    <div id="wrapper">
        <div id="page">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">訂單查詢結果</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <form id="mainForm" action="/member/login1" method="POST">
            <!-- /.row -->
            <% if(loginStatus == false) {%>
                <% include loginlayout %>
            <% }else if(loginStatus == true) { %>
            <div class="row wrap1280">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            訂單資訊
                        </div>
                        <!-- /.panel-heading -->
                            <div id="panBody1" class="panel-body">
                                <table id="order_list1" width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                                    <thead>
                                        <tr>
                                            <th>訂單編號</th>
                                            <th>下單時間</th>
                                            <th>收件人</th>
                                            <th>收件地址</th>
                                            <th>總金額</th>
                                            <th>訂單狀態</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% for(var i=0 ; i < data.length;i++) { %>
                                            <tr class="odd gradeX">
                                                <td>
                                                    <%=data[i].order_id %>
                                                    <input type="hidden" name="orderId" value="<%=data[i].order_id %>">
                                                </td>
                                                <td>
                                                    <%=moment(data[i].addtime).format('YYYY-MM-DD') %>
                                                </td>
                                                <td>
                                                    <%=data[i].buyer %>
                                                </td>
                                                <td>
                                                    <%=data[i].address %>
                                                </td>
                                                <td>
                                                    <%=data[i].total%>
                                                </td>
                                                <td>
                                                    <%=statusArray[i] %>
                                                </td>
                                            </tr>
                                            <% } %>
                                    </tbody>
                                </table>
                                <!-- /.table-responsive -->
                            </div>
                            <!-- /.panel-body -->
                        </div>
                        <!-- /.panel -->				                    
                    </form>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->

            <div class="row wrap1280">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            商品明細
                        </div>
                        <!-- /.panel-heading -->
                        <div id="panBody1" class="panel-body">
                            <table id="order_list1" width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                                <thead>
                                    <tr>
                                        <th>圖片</th>
                                        <th>商品名稱</th>
                                        <th>售價</th>
                                        <th>購買數量</th>
                                        <th>小計金額</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% for(var i=0 ; i < data2.length;i++) { %>
                                        <tr class="odd gradeX">
                                            <td>
                                                <img src="<%=data2[i].pic %>" alt="">
                                            </td>
                                            <td>
                                                <%=data2[i].product_name %>
                                            </td>
                                            <td>
                                                <%=data2[i].price %>
                                            </td>
                                            <td>
                                                <%=data2[i].amount %>
                                            </td>
                                            <td>
                                                <%=data2[i].subtotal %>
                                            </td>
                                        </tr>
                                        <% } %>
                                </tbody>
                            </table>
                            <!-- /.table-responsive -->
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                </form>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <% } %>
        <script>
            $(function () {
                // var orderId = $('#orderId').text().replace(/  /g,"");
            })

        </script>
</body>
</html>